# Include base dependencies
-r requirements.txt

# Dev-only dependencies
pytest==9.0.3
pytest-cov==7.1.0
freezegun>=1.5
factory-boy>=3.3
black>=24.0
flake8>=7.0
ruff>=0.15          # leanness gate: over-engineering / non-native patterns
vulture>=2.16       # leanness gate: unused functions / classes / methods
pip-audit>=2.7
pre-commit>=3.0
pytest-asyncio>=0.23
# Security scanning tools (ds-quality:security static passes — brownfield slice 18.x)
bandit>=1.7
semgrep>=1.60

# mcp: floor past CVE-2026-52870 / -52869 / -59950 (all fixed by 1.28.1). Transitive via
# semgrep + fastmcp; pip resolves mcp-1.28.1 cleanly with no parent-cap conflict. Keeps the
# pip-audit / Full CI security gate green. WO-DEP-CVE-BUMP (7f9085e7).
mcp>=1.28.1

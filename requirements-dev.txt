# Include base dependencies
-r requirements.txt

# Dev-only dependencies
pytest==9.0.3
pytest-cov==7.1.0
freezegun>=1.5
factory-boy>=3.3
black>=24.0
flake8>=7.0
ruff>=0.15          # leanness gate: over-engineering / non-native patterns
vulture>=2.16       # leanness gate: unused functions / classes / methods
pip-audit>=2.7
pre-commit>=3.0
pytest-asyncio>=0.23
# Security scanning tools (ds-quality:security static passes — brownfield slice 18.x)
bandit>=1.7
semgrep>=1.60

# mcp: CANNOT floor past CVE-2026-52870 / -52869 / -59950 (fixed in 1.28.1). semgrep caps mcp
# at an older release (==1.23.3), so `mcp>=1.28.1` forces pip to backtrack semgrep down to a
# source-only version with no Windows wheel ("Semgrep does not support Windows yet") — it broke
# the Windows pr-smoke install. Same constraint class as the PyJWT note in requirements.txt. The
# 3 mcp CVEs are dev-only (transitive via semgrep/fastmcp) and remain flagged by pip-audit; the
# security-gate suppression is handled with the Full-CI recovery in WO ac814dc3, not here.
# Revisit when semgrep relaxes its mcp pin. WO-DEP-CVE-BUMP (7f9085e7).

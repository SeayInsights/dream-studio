# ds-workorder — Work Order Lifecycle

**Type:** Function-backed skill pack
**Invocation:** matched by per-mode triggers in `modes/*/metadata.yml`
**Not a CLI command.** The AI invokes one of the five modes below by calling the named function in `core.work_orders.*` and presenting the returned dict to the user.

The Dream Studio source-of-truth is the SQLite authority. This pack does not reason about work-order state from session memory or training knowledge. Every mode calls a named query or mutation function and surfaces its returned data.

---

## Mode dispatch

| Mode | File | Wraps | Keywords |
|------|------|-------|----------|
| start | `modes/start/SKILL.md` | `core.work_orders.start.start_work_order` | start work order:, begin work order: |
| execute | `modes/execute/SKILL.md` | `core.work_orders.mutations.mark_task_done` | mark task done:, task done:, complete task: |
| close | `modes/close/SKILL.md` | `core.work_orders.close.close_work_order` (+ `check_close_gates`) | close work order:, finish work order: |
| block | `modes/block/SKILL.md` | `core.work_orders.mutations.block_work_order` | block:, blocked by: |
| status | `modes/status/SKILL.md` | `core.work_orders.queries.list_work_orders` (+ `list_tasks`) | work order status:, show tasks: |

---

## Rules that apply to every mode

1. **Read functions before you write.** If a state-surfacing instruction below does not name the specific query function being called, stop and add it. Never narrate state from session context or training data.
2. **Present returned dicts. Don't invent fields.** The function returns a dict with a known shape. Show those fields. If the user asks for a field that isn't there, say so — don't fabricate it.
3. **Mutations require explicit user confirmation.** start, execute, close, block all mutate state. Confirm intent before calling the function, then call it, then show the user the returned dict so they can verify the change.
4. **Errors are operator-visible.** When a function returns `ok=False`, surface the `error` field verbatim. Do not paraphrase or soften the message.
5. **No raw UUIDs to the user unless asked.** Use the work_order_id internally; refer to the work order by its `title` in conversation. The user can ask for the ID.
6. **Task descriptions should include behavioral acceptance criteria.** When authoring tasks (via scope or during a WO), each task description should include an "Acceptance:" clause stating what the operator observes when the task is done. The independent review completion grader will emit a warning gap if feature/infrastructure WOs have no observable behavioral AC — this is not a fail, but it signals that the grader could not verify expected operator outcomes.

7. **SQL-CHECK lines for DB-state ACs.** A task's `acceptance_criteria` field may include one or more `SQL-CHECK:` lines (format: `SQL-CHECK: <SELECT statement>`). The completion grader executes these read-only against the authority DB before analyzing the diff. A query returning a non-zero/non-null first column passes; zero/null/no rows or any error fails. A `SQL-CHECK RESULT: FAIL` annotation forces that task to verdict `"missing"` regardless of diff evidence. Use SQL-CHECK when the task outcome is a database state change not visible in the diff (e.g. a migration row, a column added by bootstrap). See `docs/authoring/work-orders.md` for the full convention.

8. **Executable acceptance criteria BLOCK close (always-on AC gate).** `close_work_order` runs every task's `acceptance_criteria` as executable checks and blocks close on any failure, regardless of WO type. Three line-prefixes are supported: `SQL-CHECK: <SELECT>` (read-only DB query, passes on truthy first column), `TEST-CHECK: <pytest-node-id>` (runs that pytest node in a subprocess; passes iff exit 0), and `API-CHECK: <METHOD path -> [status]>` (boots the API via TestClient; passes on 2xx/expected status + non-empty body). A WO **cannot close without `force=True` unless it has at least one executable check** across its tasks — author at least one SQL/TEST/API-CHECK AC per WO. `force=True` bypasses and emits `gate.bypassed`. The legacy `all_tests_pass` gate now executes the WO's TEST-CHECKs instead of grepping `test-results.md`.

9. **Every task must be done before close (tasks_done gate).** `close_work_order` blocks the close when any task is not `complete` (or deliberately `cancelled`) — no 0/N or partial closes ("NOTHING LEFT HANGING"). Mark each task via the execute mode (`mark_task_done`) before closing; `force=True` bypasses and records a `gate.bypassed` event carrying the `tasks_done` reason. Both the CLI close path and the autonomous execute-work-orders loop go through the same `close_work_order`, so enforcement is identical everywhere. Close also runs a projection tick before reading task statuses, so a freshly marked-done task is never misread as still-pending.

10. **Escalation ladder (escalated WOs).** When the deterministic verifier/outcome-eval says a closed WO is NOT FIXED, the platform reopens it and escalates (`core/work_orders/escalation.py`, table `ds_escalations`): (a) the retry is routed to a more capable model — `resolve_executor` returns `opus` for an escalated WO; both the autonomous loop (the `escalation-probe` node + `ds work-order executor <id>`) and the manual path (`start_work_order`'s `executor` field) honor it. (b) Re-close REQUIRES a *passing* independent review — for an escalated WO the `independent_review` gate cannot be bypassed by unreviewable/gap shortcuts OR by `force=True` (`close_work_order` returns `escalated: True` and refuses). (c) Retries are capped (`escalation.retry_cap`, env `DREAM_STUDIO_ESCALATION_RETRY_CAP` > `ds_config` > default 3); at the cap the ladder stops auto-reopening and writes an `ESC-RETRYCAP-*` operator escalation — no silent loop.

<!-- Last reviewed 2026-06-12 — WO-SPAWN-DEDUPE: _insert_gap_work_orders in core/work_orders/verify.py gains title-match dedup; merges tasks into existing open WO instead of spawning duplicate. merged_into_existing field added to result. No skill instruction change required — existing mutation discipline unchanged. -->

<!-- Last reviewed 2026-06-12 — WO-VIEW-GHOSTS: no work-order skill instruction change. core/work_orders/verify.py _CORRECTNESS_PROMPT_TEMPLATE gains rule (8) for dead-table resurrection detection. Skill surface and mutation discipline unchanged. -->

<!-- Last reviewed 2026-06-12 — WO-2dbcdc63: core/work_orders/verify.py _find_migration_files gains Path(source_root) coercion to handle os.getcwd() returning str. Pure bug fix, no skill interface change. -->

<!-- Last reviewed 2026-06-13 — WO-AUTHORITY-GRADER: core/work_orders/verify.py gains _run_sql_checks() and _format_sql_checks(). SQL-CHECK: lines in acceptance_criteria are now executed against the authority DB before graders run. Results injected into task_list_str as ground truth. Rule 7 added above; docs/authoring/work-orders.md created. -->

<!-- Last reviewed 2026-06-13 — WO-VERIFY-NOSUMMARY: core/work_orders/verify.py _collect_grader now treats empty/whitespace LLM output as unreviewable ({"unreviewable": True, "reason": "grader_no_summary"}) instead of raising ValueError. _run_graders_parallel retries once on unreviewable with a 30s timeout. verify_work_order detects unreviewable graders after retry and writes an unreviewable verdict (unreviewable_graders: [...]) mirroring the existing no-commits-found path — close proceeds without force. close_work_order surfaces unreviewable_graders in result when set. The independent_review gate already passes on unreviewable: True. No skill surface change. -->

<!-- Last reviewed 2026-06-14 — WO-SYMPTOM-RESOLUTION (feat/symptom-resolution): close_work_order() in core/work_orders/close.py gains _check_originating_symptom() — a new implicit gate that re-runs the originating_symptom SQL-CHECK after regular gate evaluation. If the symptom SQL still returns 0/falsy (or errors), close is blocked with failures=[...originating_symptom...]. Bypassed by force=True consistent with other gates. mutations.py gains: (1) originating_symptom param on create_work_order() — included in the work_order.created event payload and materialized by WorkOrderProjection; (2) set_originating_symptom(work_order_id, symptom, source_root) — direct UPDATE for post-creation backfills. Skill surface unchanged: close mode still calls close_work_order(); mutations mode still calls create_work_order() and mark_task_done(). No new mode, no new routing keyword, no new CLI subcommand added. -->

<!-- Last reviewed 2026-06-15 — WO-AC-EXECUTABLE (feat/ac-executable-gate): core/work_orders/verify.py gains run_executable_checks() dispatching SQL-CHECK / TEST-CHECK (pytest node via subprocess, sys.executable) / API-CHECK (FastAPI TestClient), fail-closed on unknown/malformed CHECK lines. core/work_orders/close.py wires an always-on AC gate (_run_ac_gate + _read_wo_tasks) into both close_work_order and check_close_gates regardless of WO type: any failing executable check blocks close, and a WO with zero executable checks cannot close without force=True. The all_tests_pass gate now executes TEST-CHECKs (run_gate_check gains optional db_path) instead of grepping test-results.md. force=True bypasses and emits gate.bypassed per failure. Rule 8 added above. Skill surface (modes/routing/CLI) unchanged — close mode still calls close_work_order(). -->

<!-- Last reviewed 2026-06-15 — WO-REVIEW-TRACEABILITY (feat/review-traceability): the independent-review grader now keys off DB identity (verify_work_order already receives work_order_id; it grades that WO's tasks + ACs regardless of commit/artifact parsing). core/work_orders/verify.py _collect_git_commits is best-effort EVIDENCE only — matches commits by full uuid OR short(8) id OR a 'Work-Order: <uuid>' trailer (commit body, via git log --format=%H --grep) OR branch name; absence of commits is "no evidence", never a certified pass or auto score-0. core/work_orders/close.py: an unreviewable independent_review verdict is NO LONGER a certified pass (run_gate_check returns an advisory 'independent_review: unreviewable' failure instead of passing); close_work_order bypasses that advisory failure ONLY when the always-on executable-AC gate passes (unreviewable + passing AC → closes; unreviewable + no/failing AC → blocked by the AC gate). This closes the #358 false-done hole while keeping the AC gate authoritative. Skill surface (modes/routing/CLI) unchanged — close mode still calls close_work_order(). -->

<!-- Last reviewed 2026-06-15 — WO-TASKDONE-SYNC (fix/taskdone-sync): core/work_orders/mutations.py mark_task_done() now calls sync_tick() inline after emitting the task.completed event, mirroring create_task/create_work_order. It was the only SDLC mutation omitting the sync, so business_tasks.status (and list_tasks / `ds work-order tasks`) reported a stale 'pending' until an unrelated projection tick ran. The execute mode's Side effects section is updated: the read model now reflects status 'complete' before mark_task_done() returns; no separate sync step is needed. Skill surface (modes/routing/CLI) unchanged — execute mode still calls mark_task_done(). -->

<!-- Last reviewed 2026-06-16 — de04c240 (Rule 3 fix): core/work_orders/mutations.py gains reopen_work_order(work_order_id, reason, source_root, dream_studio_home) — sets a closed WO back to in_progress, emits work_order.reopened, and syncs the read model. Added so the outcome eval (core/eval/runner.py) reopens a regressed WO through the designated business-state writer instead of a raw UPDATE from the eval layer (the correctness grader's Rule 3 finding). Skill surface unchanged — no new mode, routing keyword, or CLI subcommand; execute/close modes still call mark_task_done()/close_work_order(). -->

<!-- Last reviewed 2026-06-16 — WO-TASKS-DONE-ENFORCE (feat/tasks-done-enforce): core/work_orders/close.py close_work_order() gains an always-on tasks_done gate — any task not in ('complete','cancelled') blocks close unless force=True (no 0/N or partial closes), and a forced close records the bypass via the existing gate.bypassed path. A sync_tick() now runs before the task-status read so a freshly marked-done task is not misread as pending (projection lag). Enforcement lives in the single shared close_work_order, so the CLI close path and the autonomous execute-work-orders loop inherit it identically. Rule 9 added above; close/execute mode Side effects sections updated. Skill surface (modes/routing/CLI) unchanged — close mode still calls close_work_order(). -->

<!-- Last reviewed 2026-06-17 — WO-ESCALATION-LADDER (feat/escalation-ladder): new core/work_orders/escalation.py (compute_not_fixed_signal + not_fixed_for_work_order from T1, plus mark_escalated/resolve_executor/read_escalation/get_retry_cap/register_retry/escalate_to_operator) backed by migration 126 ds_escalations (work_order_id PK, escalation_level, retry_count, designated_executor, last_reason). core/eval/runner.py _reopen_and_escalate now registers a retry, escalates to the operator (ESC-RETRYCAP-*) when the cap is hit instead of silently reopening, and otherwise marks the reopened WO escalated → Opus. core/work_orders/close.py: escalated WOs require a PASSING independent review to re-close — the gaps/unreviewable bypasses are skipped and force cannot bypass the independent_review gate (returns escalated: True). core/work_orders/start.py surfaces the resolved executor (+ escalation block) so the manual path honors routing; new CLI `ds work-order executor <id>` + an escalation-probe node in execute-work-orders.yaml so the autonomous loop honors the same flag. Rule 10 added above. New CLI subcommand `work-order executor` (read-only resolver) — no new mode or routing keyword. -->

<!-- Last reviewed 2026-06-17 — WO-CLOSE-AUTOSTART-PARITY (fix/close-autostart-parity): core/work_orders/close.py close_work_order() now advertises the SAME next WO it auto-starts. Previously the auto-start used the project-wide ready-set selector (get_next_work_order — respects cross-milestone order, deps, startability) while next_work_order/next_block came from a naive same-milestone status='created' sequence query that could name a WO start_work_order refuses (earlier milestone still open). Fix: on the verify-passed auto-start path, the ready-set pick is auto-started AND next_work_order/next_command/next_block are overwritten to that WO, so advertised == started. No skill-surface, mode, routing, or CLI change — close mode still calls close_work_order(). -->

<!-- Last reviewed 2026-06-17 — WO-GATE-HARDEN-CLEANUP (fix/gate-harden-cleanup): internal hardening of two work-order engine files, NO skill-surface change. core/work_orders/verify.py _find_migration_files now resolves each git-diff filename and confirms containment inside the migrations dir before use (path-traversal defense). core/work_orders/escalation.py mark_escalated raises RuntimeError instead of AssertionError when the row is unreadable after the upsert. No mode, routing keyword, CLI surface, gate, or close/verify behavior change — close still requires a passing review, gates are unchanged. -->


<!-- Last reviewed 2026-06-19 — WO-LIVE-DATA-GATE: core/work_orders/verify.py SQL-CHECK now hard-fails on zero rows (SELECT 1 WHERE convention); core/work_orders/close.py adds a dashboard-truth live-authority gate for dashboard/telemetry/data_pipeline/saas_feature WO types only (other types, incl. infrastructure/cleanup, unaffected). No ds-workorder skill mode/routing/CLI change; close still calls close_work_order(). -->

<!-- Last reviewed 2026-06-20 — WO-P20-CLOSE-LAG (fix/wo-p20-close-lag): core/work_orders/close.py close_work_order() now calls sync_tick() after emitting the work_order.closed spool event (post-close flush), supplementing the existing pre-close sync_tick that flushes pending task.completed events before reading gate state. The direct business_work_orders UPDATE already reflects the closed status; this post-close flush makes the WorkOrderProjection record immediately consistent too. No mode, routing keyword, CLI surface, gate, or skill-surface change — close mode still calls close_work_order(). -->

<!-- Last reviewed 2026-06-20 — WO-SPAWN-LOOP-FIX (fix/verify-spawn-loop-dedup): core/work_orders/verify.py _insert_gap_work_orders() dedup now keys on a stable gap key (reviewed_work_order_id + gap category) stored as a [gap-key: ...] marker on the spawned WO, scoped by project_id (not milestone_id), and applied across ANY status — so re-reviews never spawn duplicate remediation WOs even when the grader rewords the title, and a gap key already spawned (incl. closed) is suppressed not re-spawned. New _filter_invented_threshold_gaps() drops gaps citing numeric thresholds (line counts, coverage %, etc.) absent from the task acceptance-criteria text; the completion grader prompt is constrained to match and now emits a stable `category` per gap. The grader signature gains required reviewed_work_order_id; the call site relaxes the milestone_id requirement. No ds-workorder skill mode/routing/CLI change — verify mode still calls verify_work_order(); close still calls close_work_order(). -->

<!-- Last reviewed 2026-06-22 — WO a48ec9a2 (fix/wo-verify-utf8-encoding): core/work_orders/verify.py now pins encoding="utf-8", errors="replace" on all 6 subprocess.run/Popen calls (the claude --print grader Popen, the TEST-CHECK pytest runner, and the git log/branch/show evidence reads). Previously text=True with no encoding decoded as cp1252 on Windows, so a non-cp1252 byte in grader/diff output (e.g. 0x90) raised UnicodeDecodeError in the reader thread, losing a grader and yielding a degraded passed=False verdict with phantom remediation gap WOs. Pure reliability fix; an AST guard test in tests/unit/test_wo_verify.py now asserts every subprocess call sets encoding. No ds-workorder skill mode/routing/CLI change — verify mode still calls verify_work_order(); close still calls close_work_order(). -->

<!-- Last reviewed 2026-06-23 — WO c68f9e14 (fix/preflight-gate-uses-resolved-db-path): core/work_orders/start.py _check_preflight_gate and _get_pending_audits_for_project now take an explicit db_path and read it via _connect(db_path) instead of resolving an ambient connection through core.config.database.get_connection(). start_work_order passes the already-resolved _db_path_for_seq to both, so the preflight gate and the pending-audits advisory inspect the same authority DB the work order lives in (matching _check_sequence_order's existing signature). Behaviour-preserving in single-DB production (ambient == the WO's DB); fixes a cross-DB hazard where the gate could inspect a different database than the one the WO lives in (surfaced under multi-home/test contexts). Both are private helpers — no mode, routing keyword, CLI surface, gate, or start_work_order return-shape change; start mode still calls start_work_order(). -->


<!-- Last reviewed 2026-06-27 — Wave 2 substrate realignment (migration 131, worktree-agent-a910d590fedb5c672): core/work_orders/start.py: the pending_audits advisory is removed — migration 131 drops the pending_audits table (its only writer, defer_project_audit(), was never called) so _get_pending_audits_for_project() and its start_work_order call site are deleted. No ds-workorder skill mode, routing keyword, CLI surface, gate, or start_work_order return-shape change; the preflight gate and sequence-order checks are unchanged. -->

<!-- Last reviewed 2026-07-03 — WO-DBA-EVAL-DECISION (fcd8cf14) PR A: core/work_orders/verify.py _write_eval_run now also persists the verdict summary to business_work_orders (verify_status passed|failed|unreviewable, verify_score, verified_at — migration 134) and emits a work_order.verified canonical event through the spool→ingestor path; the legacy ds_eval_runs insert remains until T4 drops the table. No ds-workorder skill mode, routing keyword, CLI surface, gate, or verify_work_order return-shape change — verify mode still calls verify_work_order() and reads review-verdict.json for the close gate. -->

<!-- Last reviewed 2026-07-03 — WO-DBA-EVAL-DECISION (fcd8cf14) PR B / T4: core/work_orders/verify.py _write_eval_run no longer inserts into ds_eval_runs (migration 136 drops the table) — the business_work_orders verify_status/verify_score/verified_at UPDATE and the work_order.verified canonical event emission are unchanged and remain the durable record. No ds-workorder skill mode, routing keyword, CLI surface, gate, or verify_work_order return-shape change. -->

<!-- Last reviewed 2026-07-04 — WO-FIX-VERIFY-GATE (4174d0e9): core/work_orders/verify.py::verify_work_order no longer declares a WO "unreviewable" solely because commit-grep by WO id/title found nothing (squash merges strip the id). It now falls back to the branch working-diff vs origin/main, then to the executable AC results (SQL/TEST/API-CHECK) as objective authority evidence, and records certification_basis (git_diff|authority_evidence) in the verdict. Only genuinely-no-evidence stays unreviewable (no-false-done preserved). No ds-workorder mode, routing, or CLI-surface change — verify still calls verify_work_order() and close still reads review-verdict.json. -->

<!-- Last reviewed 2026-07-04 — WO-FIX-VERIFY-GATE (4174d0e9) follow-up (grader-guard): supersedes the working-diff clause above. The whole-repo working-diff fallback was removed (it picked up ambient repo changes unrelated to the WO); authority-evidence (the WO's own executable AC results) is now the sole no-commits fallback. The authority-evidence fallback is additionally gated two ways: (1) it only runs when the `claude` grader CLI is available (or DREAM_STUDIO_VERIFY_MOCK is set) — with no grader, verify degrades to an unreviewable verdict instead of spawning a grader that crashes (the CI FileNotFoundError that turned main red post-#467); (2) it is suppressed for escalated work orders — a regressed WO must earn a genuine re-review and cannot re-certify from the same executable AC. No ds-workorder mode, routing, or CLI-surface change. -->

<!-- Last reviewed 2026-07-04 — WO-FIX-VERIFY-GATE (4174d0e9) follow-up 2 (spawn-guard): corrects clause (1) of the note above. The grader-availability check is no longer a `shutil.which("claude")` gate in verify_work_order's main flow — that gate short-circuited before the grader/gap-spawning path and broke every test that mocks _run_graders_parallel/_collect_grader (test_wo_verify, test_behavioral_ac_check) on a claude-less CI runner. Grader-absence is now handled at the real crash site: _run_graders_parallel catches FileNotFoundError from _spawn_grader per grader and marks that grader unreviewable (reason grader_cli_unavailable), which flows through the existing unreviewable-graders verdict path (no false-done preserved). The authority-evidence fallback again runs whenever there are no commits and the WO is not escalated; only the missing-CLI handling moved. No ds-workorder mode, routing, or CLI-surface change. -->

<!-- Reviewed 2026-07-05 — WO 6d978483 (PEP 585/604 modernization [2/2]): source files in this domain received mechanical type-annotation modernization only (PEP 585 builtin generics, PEP 604 unions, datetime.UTC) via ruff UP safe autofixes. No contract, behavior, schema, routing, API-shape, or CLI-surface change — reviewed, no doc content change needed. -->

<!-- Reviewed 2026-07-05 — WO-GAP-DEDUPE-CLASS (a4d66ef7): core/work_orders/verify.py gap-spawn dedup now treats a small allowlist of generic advisory categories (_ADVISORY_PROJECT_WIDE_CATEGORIES = {missing-behavioral-ac}) as project-wide — _gap_key drops the reviewed-WO id for those so the class spawns at most one open tracking WO instead of a near-duplicate per reviewed WO (13 had accumulated). Content-specific categories (missing-tests, task-N-incomplete) stay scoped to the reviewed WO. No ds-workorder mode, routing, CLI-surface, or verify return-shape change. -->

<!-- Reviewed 2026-07-05 — WO-GRADER-RETRY-NONJSON (ae2be823): core/work_orders/verify.py::_run_graders_parallel now retries a grader once on non-JSON output (_grader_error), not only on empty output (unreviewable). A prose reply previously defaulted the grader score to 0.0 and false-FAILed the WO. The grader_cli_unavailable case is explicitly NOT retried (a re-spawn cannot recover a missing binary). No ds-workorder mode, routing, CLI-surface, or verify return-shape change. -->

<!-- Reviewed 2026-07-06 — WO-SPLIT-HANDOFF (e3a0f7f3): core/work_orders/handoff.py (5,253 lines) was split into handoff_constants / handoff_helpers / handoff_decision / handoff_build / handoff_validate / handoff_security behind a facade (handoff.py) that re-exports the full public API via __all__, so every `from core.work_orders.handoff import X` caller is unchanged. The build<->validate cycle (build_handoff_sections calls self_validate_generated_handoff; validate calls build_handoff_prompt) is broken with a function-local import in build. No ds-workorder mode, routing, CLI-surface, or behavior change. -->

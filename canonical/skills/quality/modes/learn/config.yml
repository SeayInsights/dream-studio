name: learn
model_tier: haiku
description: 'Capture and promote lessons from builds — record to raw_lessons DB,
  Director review via lesson_queue, promote to memory / skill / agent updates.
  Trigger on `learn:`, `capture lesson:`, or `learn: harvest` for cross-project batch
  extraction.'
pack: quality
chain_suggests: []
